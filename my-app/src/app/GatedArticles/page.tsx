// import { Article } from "@/components/Article";

export default function Page() {
  return (
    <>
      <div>
        asdf
        {/* {articles?.map((article: any) => (
          <Article
            key={article.id}
            id={article.id}
            title={article.title}
            text={article.text}
          />
        ))} */}
      </div>
    </>
  );
}
